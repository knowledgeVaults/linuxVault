# Linux: Disk Free Command

The `df` command that shows the user how much disk space is currently being used and available on the server's filesystems

* Reports disk space statistics for mounted filesystems