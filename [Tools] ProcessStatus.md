# Linux: Process Status

`ps` is a tool that reports a snapshot of the current status of processes running on Unix-like systems

* Lists processes associated with the current terminal by default (Without options)